dependencies:
  cache_directories:
    - "~/.stack"
    - "~/.local/bin/"
  override:
    - stack upgrade
    - ~/.local/bin/stack setup
    - ~/.local/bin/stack build --pedantic
test:
  override:
    - stack test --pedantic
